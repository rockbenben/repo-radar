{
  "name": "@repo-radar/server",
  "private": true,
  "description": "repo-radar backend: scans and watches local Git repos over Hono + WebSocket, all git calls via spawn.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.14",
    "@hono/node-ws": "^1.3.1",
    "chokidar": "^5.0.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
